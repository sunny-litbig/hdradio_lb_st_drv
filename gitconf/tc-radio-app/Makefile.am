##############################################
AUTOMAKE_OPTIONS = foreign
##############################################

#defines for AARCH64
if SYSTEM_IS_AARCH64
TOP_BIN = ./bin64
else
TOP_BIN = ./bin
endif

BIN_APP = $(TOP_BIN)/TcRadioApp

addappdir = ${bindir}
addapp_SCRIPTS = $(BIN_APP) 
